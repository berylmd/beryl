{
  "name": "@repo/beryljs",
  "type": "module",
  "module": "dist/index.js",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "declaration": true,
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate": "nearleyc parser.ne -o grammar.ts",
    "build": "pnpm run generate && tsc",
    "dev": "concurrently \"nodemon --watch parser.ne --ext ne --exec 'pnpm run generate'\" \"tsc --watch\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/nearley": "^2.11.4",
    "nearley": "^2.20.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14",
    "vitest": "latest"
  },
  "exports": {
    ".": "./dist/index.js"
  }
}
